// import { createSlice } from "@reduxjs/toolkit";

// export const favoritePhotosSlice = createSlice({
//     name: "favoritePhotos",
//     initialState: [],
//     reducers: {
//         addFavorite: (state, action) => {
//             return [...state, action.payload]
//         },
//         removeFavorite: (state, action) => {
//             return state.filter(photo => photo.id !== action.payload.id)
//         }
//     }
// });

// export const selectFavoritePhotos = (state)=> state.favoritePhotos;

// export const {addFavorite, removeFavorite } = favoritePhotosSlice.actions;

// export default favoritePhotosSlice.reducer;
